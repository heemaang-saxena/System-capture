{
  "name": "system-capture",
  "version": "1.0.0",
  "description": "System audio capture native addon",
  "main": "index.js",
  "dependencies": {
    "bindings": "^1.5.0",
    "ffmpeg": "^0.0.4",
    "node-addon-api": "^7.0.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "node-gyp": "^10.0.1"
  },
  "scripts": {
    "install": "node-gyp rebuild",
    "rebuild": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "configure": "node-gyp configure",
    "build": "node-gyp build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
